# Construct an empty data frame of menu metadata

Construct an empty data frame of menu metadata

## Usage

``` r
empty_menu_metadata_table()
```

## Value

A default blank data.frame with columns `html_file`, `title`, and
`index` for each md file in the menu subdirectory.
