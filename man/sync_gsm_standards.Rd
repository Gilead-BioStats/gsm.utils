% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync_gsm_standards.R
\name{sync_gsm_standards}
\alias{sync_gsm_standards}
\title{Sync GSM standards (AI docs + issue templates + CI workflows)}
\usage{
sync_gsm_standards(
  strPackageDir = ".",
  mode = c("write", "check"),
  overwrite_ai_docs = FALSE,
  overwrite_actions = TRUE,
  dry_run = FALSE,
  ai_docs_dir = file.path(".github", "ai"),
  fail_on_drift = FALSE
)
}
\arguments{
\item{strPackageDir}{Path to the target package repo (default: ".").}

\item{mode}{One of \code{"write"} or \code{"check"}.}

\item{overwrite_ai_docs}{Logical; overwrite existing AI docs/templates in \code{mode = "write"} (default: \code{FALSE}).}

\item{overwrite_actions}{Logical; overwrite existing \code{.github/workflows} in \code{mode = "write"} (default: \code{TRUE}).}

\item{dry_run}{Logical; in \code{mode = "write"}, preview AI docs changes without writing files (default: \code{FALSE}). Workflows are not updated when \code{dry_run = TRUE}.}

\item{ai_docs_dir}{Relative directory under \code{strPackageDir} where AI docs are synced. Default is \code{.github/ai}. Use \code{"."} to sync at repo root.}

\item{fail_on_drift}{Logical; in \code{mode = "check"}, error when AI docs drift (excluding \code{ARCHITECTURE.md} and issue templates) or workflow drift is detected.}
}
\value{
Invisibly returns a list with components:
\itemize{
\item \code{ai_docs}: AI docs drift report (\code{data.frame}) in check mode, or destination paths in write mode
\item \code{gha}: workflow drift summary in check mode, or logical copy result in write mode
}
}
\description{
Provides a one-command sync/check entry point for repository standards.
In \code{mode = "write"}, it syncs AI docs (and issue templates) plus GitHub
Actions workflow templates. In \code{mode = "check"}, it reports drift for AI
docs and workflow versions. Drift checks intentionally ignore GitHub issue
templates and allow \code{ARCHITECTURE.md} to vary by repository.
}
