% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_pr_message.R
\name{build_pr_message}
\alias{build_pr_message}
\title{Build PR message and optionally create a GitHub PR}
\usage{
build_pr_message(
  overview,
  test_notes = character(0),
  connected_issues = character(0),
  run_gh = FALSE,
  pr_title = NULL,
  base = "dev",
  head = NULL,
  repo = NULL,
  draft = FALSE
)
}
\arguments{
\item{overview}{\code{character} vector describing what changed.}

\item{test_notes}{\code{character} vector with test notes/commands. Optional.}

\item{connected_issues}{\code{character} vector of issue references (e.g., \code{"123"}, \code{"#123"}, \code{"Closes #123"}). Optional.}

\item{run_gh}{\code{logical} toggle to run GitHub CLI PR creation. Default is \code{FALSE}.}

\item{pr_title}{Optional PR title. If \code{NULL}, a title is inferred from the current branch name.}

\item{base}{Base branch for PR creation when \code{run_gh = TRUE}. Default \code{"dev"}.}

\item{head}{Optional head branch for PR creation when \code{run_gh = TRUE}.}

\item{repo}{Optional \code{owner/repo} for PR creation when \code{run_gh = TRUE}.}

\item{draft}{\code{logical} create PR as draft when \code{run_gh = TRUE}.}
}
\value{
Invisibly returns a list with \code{body}, \code{title}, \code{gh_called}, and \code{gh_output}.
}
\description{
Creates a pull request body aligned to the repository PR template and can
optionally invoke \code{gh pr create}.
}
