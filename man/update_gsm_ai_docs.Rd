% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_gsm_ai_docs.R
\name{update_gsm_ai_docs}
\alias{update_gsm_ai_docs}
\title{Update a GSM package with standard "AI-ready" context docs}
\usage{
update_gsm_ai_docs(
  strPackageDir = ".",
  overwrite = FALSE,
  mode = c("write", "check"),
  dry_run = FALSE,
  include = NULL,
  include_issue_templates = TRUE,
  ai_docs_dir = file.path(".github", "ai"),
  fail_on_drift = FALSE
)
}
\arguments{
\item{strPackageDir}{Path to the target package repo (default: ".").}

\item{overwrite}{Logical; overwrite existing files (default: FALSE).}

\item{mode}{One of \code{"write"} or \code{"check"}. \code{"write"} copies files according to \code{overwrite}. \code{"check"} does not write and returns drift report only.}

\item{dry_run}{Logical; when \code{TRUE} and \code{mode = "write"}, report what would change without writing files.}

\item{include}{Optional character vector of relative template paths to sync. By default all templates are processed.}

\item{include_issue_templates}{Logical; when \code{TRUE} (default), also sync \code{.github/ISSUE_TEMPLATE/*} from \code{inst/gha_templates/ISSUE_TEMPLATE}.}

\item{ai_docs_dir}{Relative directory under \code{strPackageDir} where AI docs are synced. Default is \code{.github/ai}. Use \code{"."} to sync at repo root.}

\item{fail_on_drift}{Logical; when \code{TRUE}, error if any files are missing or differ from templates.}
}
\value{
Invisibly returns:
\itemize{
\item in \code{mode = "write"}: destination paths (character vector)
\item in \code{mode = "check"}: drift report (\code{data.frame})
}
}
\description{
Copies template markdown and GitHub meta files from gsm.utils into a target
repository directory.
}
