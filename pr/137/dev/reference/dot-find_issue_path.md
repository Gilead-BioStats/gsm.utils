# A simple path constructor for mocking

A simple path constructor for mocking

## Usage

``` r
.find_issue_path(strPackageDir)
```

## Arguments

- strPackageDir:

  (`string`) Path to the package directory.

## Value

The path to issue templates.
