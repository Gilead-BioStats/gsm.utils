# Add GSM Contributor Guidelines markdown to package

Add GSM Contributor Guidelines markdown to package

## Usage

``` r
add_contributor_guidelines(strPackageDir = ".", overwrite = TRUE)
```

## Arguments

- strPackageDir:

  (`string`) Path to the package directory.

- overwrite:

  (`boolean`) Overwrite existing files?
