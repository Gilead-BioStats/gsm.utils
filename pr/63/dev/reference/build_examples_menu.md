# Build ordered example menu

Build ordered example menu

## Usage

``` r
build_examples_menu(html_files, metadata)
```

## Arguments

- html_files:

  Character vector of example HTML files.

- metadata:

  Data frame with `html`, `title`, and `index` columns.

## Value

List of menu entries with `html` and `title`.
