# Build a title line for YAML front matter

Build a title line for YAML front matter

## Usage

``` r
.make_title_line(strTitle)
```

## Arguments

- strTitle:

  (`string`) Display title.

## Value

A single string, e.g. `'title: "My Title"'`.
