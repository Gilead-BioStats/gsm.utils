# List example metadata from Rmd files

List example metadata from Rmd files

## Usage

``` r
list_example_metadata(rmd_dir)
```

## Arguments

- rmd_dir:

  Character. Path to directory containing example Rmd files.

## Value

Data frame with `html`, `title`, and `index` columns.
